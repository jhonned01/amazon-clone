import React from "react";

const CheckoutProduc = ({ id, title, price, description, category, image }) => {
  return (
    <div>
      <h1>{title}</h1>
    </div>
  );
};

export default CheckoutProduc;
